"""Settings package.

The active settings module is selected by the ``DJANGO_SETTINGS_MODULE``
environment variable (e.g. ``config.settings.development``). This package keeps
environment-specific overrides isolated in dedicated modules.
"""
